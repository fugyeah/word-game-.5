# word-game-.5

Canonical implementation now targets Solana account-state flows in Rust under `src/features/solana_word_game`.

The original Solidity contract is preserved only for archival reference at `legacy/wordgame.sol` and is not treated as canonical runtime logic.
